#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../typescript-assistant/build/js/src/index.js" "$@"
  ret=$?
else 
  node  "$basedir/../typescript-assistant/build/js/src/index.js" "$@"
  ret=$?
fi
exit $ret
